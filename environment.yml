name: eu-ai-rag
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - langchain
      - langchain-community
      - langchain-text-splitters
      - langchain-huggingface
      - langchain-google-genai
      - chromadb
      - sentence-transformers
      - ragas
      - gradio
      - fastapi
      - uvicorn[standard]
      - pypdf
      - python-dotenv
      - google-generativeai
      - datasets
      - pandas
      - numpy
      - tqdm
      - httpx
